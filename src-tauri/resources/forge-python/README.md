# Bundled forge-python (release)

Filled by `scripts/assemble-portable-python.sh` with a frozen `src/forge_python` tree.
Only this README is tracked in git; assembled sources are gitignored.

See `docs/setup/PACKAGING.md`.
